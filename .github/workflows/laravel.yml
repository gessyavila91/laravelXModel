name: Laravel

on:
  push:
    branches: ["main", "qa", "develop","features/*"]
  pull_request:
    branches: ["main", "qa", "develop","features/*"]
permissions:
  contents: read
jobs:
  laravel-pest:
    runs-on: ubuntu-latest
    steps:
    - name: laravel-pest
      run : echo "Deploying to production..."
#  laravel-build:
#    runs-on: ubuntu-latest
#    needs: [laravel-pest]
#    steps:
#      name: laravel-deploy
#      run : echo "Deploying to production..."
#  laravel-deploy:
#    runs-on: ubuntu-latest
#    needs: [laravel-pest, laravel-build]
#    steps:
#      name: laravel-deploy
#      run: echo "Deploying to production..."
